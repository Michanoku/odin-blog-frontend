export function Login() {
  function loginAPI() {
    // This will be the function that logs the user in and does something after
  }

  return (
    <div className="userView">
    </div>
  );
}

export function Register() {
  function registerAPI() {
    // This will be the function that registers the user and does something after
  }
  return (
    <div>
    </div>
  );
}

export function Profile() {
  function updateAPI() {
    // This will be the function that updates the user and does something after
  }

  return (
    <div>
    </div>
  );
}